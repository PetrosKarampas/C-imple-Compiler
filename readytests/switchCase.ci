program checkSwitchCase
declare x,y;
#main#
{
	input(x);
	switchcase
		case(y<=0)
		{
			
			y:=x*2;
		}
		case(x>0)
		{
			print(y*3);
			y:=x*2;
		}
		case(y=2)
		{
		
		print(x+y);
		
		}
		default return(x*2);
		;
	
	return(x);
}.
