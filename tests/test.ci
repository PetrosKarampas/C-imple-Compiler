program test
	# declarations #
	declare x;
	declare i,fact;
	# main #
	{
		input(x);
		fact := 1;
		i := 1;
		if (i<=x and not[fact<>19 or x<2])
		{
			i := fact+1;
		}
		else	{
			
			x := x-1;	
			};
		
	}.
