program test1

    # declarations #
    declare x,y;
    declare a,b,c;

    # Main #
    {
        input(x);
        input(y);

        if (x>0)
        {
            while (a<b and a>c)
            {
                a := x + c;
            };
        };
        print(y)
    }.
