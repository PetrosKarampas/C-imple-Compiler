program test1

    # declarations #
    declare x,y,fact;
    declare a,b,c;

    # Main #
    {
    	x := 1;
    	y := 10;
    	a := x + 2*y;
    	b := a * a / y;
    	c := b*b;
       forcase
	case(x<10)
	{
		fact:=50;
		y:= fact - x;
		x:= x + 10;
	}
	case(y=12)
	{
		fact:=50;
		b:= x - y;
		y:= y + 1;
	}
	default
	{
		fact:=0;
	};
	print(x);
	print(y);
	print(fact);
	c := fact;
	x :=3
    }.
