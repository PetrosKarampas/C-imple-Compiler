program test2

	# declarations #
	declare x, i;
	declare count,fact;
	
	procedure popa(in x)
	{
		print(x);
		input(x);
		input (z);
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		print (z);
		input (w);
		print(sub);
		x:=b;
	}
	# main #
	{
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		popa(in x);
		print (z);
		input (w);
		print(sub);
		x := penis;
	}.
		
		
		 
