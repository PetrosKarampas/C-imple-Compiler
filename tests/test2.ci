program test2

	# declarations #
	declare x, i;
	declare count,fact;
	
	procedure popa(in x, inout b)
	{
	
		print(x);
		input(x);
		input (z);
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		print (z);
		input (w);
		print(sub);
		x:=b;
		return (z);
	
	}
	# main #
	{
		input(x);
		input (z);
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		popa(in x,inout x+3);
		print (z);
		input (w);
		print(sub);
		x := penis 
	}.
		
		
		 
