program BigTest

		declare x,y,z,a,b,c;
		declare count,mult;
	
	
	# main #
	{
		a := 20;
		x := 10;
		input(count);
		if (count < a)
		{	
			while (count<a)
			{
				
				print(a);
				if (count = 10) 
				{
					print(count);
				};
				count := count + 1;
			};
		};

		

		print(c);

        incase
		case(x <> y)
		{
			x := y;
			print(b);
		}
		case(c < 100)
		{
			input(c);
			c := c + 10;
			print(c);
		};
	}.
