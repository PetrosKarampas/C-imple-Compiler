program test

	declare a,b,c,d,e;
	
	function max(in x, in y)
	{
		if (x>y)
		{
			return (x);
		}
		else{
			return (y);
		};
	}
	function peoc(in x, in y)
	{
		return (x);
	
	}
	
	#main#
	{
		e := max(in peoc(in a, in b), in c);
	}.
