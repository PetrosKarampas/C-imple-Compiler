program test2

	# declarations #
	declare x, i;
	declare count,fact;
	
	
	
	function papa(in x, in b)
	{
	
		return (x+b);
	}
	
	procedure popa(in x, inout b, in c)
	{
		print(x);
		input(x);
		input (z);
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		print (z);
		input (w);
		print(sub);
		x:=b;
	}
	# main #
	{
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		popa(in x, inout s, in p);
		print (z);
		input (w);
		print(sub);
		x := papa(in x, in b);
	}.
		
		
