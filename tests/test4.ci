program test2

	# declarations #
	declare x, i, c, b;
	declare count,fact;
	
	
	
	function f1(in x, in b)
	{
		return (x+b);
	}
	
	procedure p1(in x, inout b, in c)
	{
		print(x);
		input(x);
		input (z);
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		print (z);
		input (w);
		print(sub);
		x:=b;
	}
	# main #
	{
		while (i<=x)
		{
			fact := fact*i;
			i := i+1;
		};
		print(fact);
		call p1(in f1(in a, in c), inout s, in c);
		print (z);
		input (w);
		print(sub);
		x := f1(in f1(in x, in s), in b);
	}.
		
		
