program test5

declare x,y,z,b,f,g;

{
	input(x);
	y:= 2;
	input(b);
	f:=y;
	g:= 10;
	
	if (b>1 or f<2 or not[g+1<f+b])
	{
		
		while(b>=10)
		{
			b := b - 10;
			if(f>2)
			{
				print(f)
			}
		}
		
	
	}
	else
	{
		input(g);
		print(g);
	
	}
	;




}.
